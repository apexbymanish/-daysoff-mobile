import 'package:flutter/material.dart';

/// Stub. See docs/stitch/screens/07-sandwich-tab.md for the spec.
class SandwichScreen extends StatelessWidget {
  const SandwichScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sandwich (stub)')),
      body: const Center(child: Text('TODO: implement sandwich timeline')),
    );
  }
}
