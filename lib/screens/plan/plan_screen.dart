import 'package:flutter/material.dart';

/// Stub. See docs/stitch/screens/05-plan-length-buffet.md for the spec.
class PlanScreen extends StatelessWidget {
  const PlanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Plan (stub)')),
      body: const Center(child: Text('TODO: implement length buffet')),
    );
  }
}
